Contains the basic stuff to make a react app.

Edit the package.json to change the name/description/version and other stuff for your app.

Use 'npm install' in a terminal to install the required stuff.